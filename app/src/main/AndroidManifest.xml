<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.trichain.hairdresser">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".user.activities.Services"/>
        <activity android:name=".user.activities.Location" />
        <activity
            android:name=".user.activities.Register"
            android:theme="@style/AppThemeMaterial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".user.activities.Login"
            android:theme="@style/AppThemeMaterial" />
        <activity android:name=".user.activities.SplashActivity" />
    </application>

</manifest>